@extends('layouts.app')

@section('content')
<div class="container-fluid px-4">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="mb-1">Vista previa KitchenDraw</h2>
            <p class="text-muted mb-0">Revisión de cabecera, ambientes, módulos y despiece antes de guardar.</p>
        </div>

        <div>
            <a href="{{ route('projects.index') }}" class="btn btn-outline-secondary">
                Volver
            </a>
        </div>
    </div>

    {{-- RESUMEN SUPERIOR --}}
    <div class="row mb-4">
        <div class="col-md-3 mb-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <small class="text-muted">Proyecto</small>
                    <h5 class="mb-0">{{ $header['project_name'] ?? '' }}</h5>
                    <small class="text-muted">N° {{ $header['estimate_id'] ?? '' }}</small>
                </div>
            </div>
        </div>

        <div class="col-md-3 mb-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <small class="text-muted">Cliente</small>
                    <h5 class="mb-0">{{ $header['client_name'] ?? '' }}</h5>
                    <small class="text-muted">{{ $header['city'] ?? '' }} / {{ $header['country'] ?? '' }}</small>
                </div>
            </div>
        </div>

        <div class="col-md-3 mb-3">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <small class="text-muted">Empresa / Diseñador</small>
                    <h5 class="mb-0">{{ $header['company_name'] ?? '' }}</h5>
                    <small class="text-muted">{{ $header['designer'] ?? '' }} - {{ $header['seller_name'] ?? '' }}</small>
                </div>
            </div>
        </div>

        <div class="col-md-3 mb-3">
            <div class="card shadow-sm h-100 bg-success text-white">
                <div class="card-body">
                    <small>Total con IVA</small>
                    <h4 class="mb-0">{{ $header['currency'] ?? '' }} {{ $header['total_with_tax'] ?? '' }}</h4>
                    <small>Estado: {{ $header['state'] ?? '' }}</small>
                </div>
            </div>
        </div>
    </div>

    {{-- ARCHIVOS --}}
    <div class="card shadow-sm mb-4">
        <div class="card-header bg-light">
            <strong>Archivos cargados</strong>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-4">
                    <small class="text-muted">XML</small>
                    <div>{{ $files['xml'] ?? '' }}</div>
                </div>
                <div class="col-md-4">
                    <small class="text-muted">TXT Despiece</small>
                    <div>{{ $files['txt'] ?? '' }}</div>
                </div>
                <div class="col-md-4">
                    <small class="text-muted">SCN Respaldo</small>
                    <div>{{ $files['scn'] ?? 'No cargado' }}</div>
                </div>
            </div>
        </div>
    </div>

    {{-- AMBIENTES --}}
    <div class="card shadow-sm mb-4">
        <div class="card-header bg-light">
            <strong>Ambientes / grupos</strong>
        </div>

        <div class="card-body p-0">
            <table class="table table-hover table-sm mb-0 align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Ambiente</th>
                        <th>Modelo</th>
                        <th>Catálogo</th>
                        <th class="text-center">Items</th>
                        <th class="text-end">Total con IVA</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach($headings as $heading)
                        <tr>
                            <td><strong>{{ $heading['name'] }}</strong></td>
                            <td>{{ $heading['model'] }}</td>
                            <td>{{ $heading['catalog'] }}</td>
                            <td class="text-center">{{ $heading['items_count'] }}</td>
                            <td class="text-end">{{ $header['currency'] ?? '' }} {{ $heading['total_with_tax'] }}</td>
                        </tr>
                    @endforeach
                </tbody>
            </table>
        </div>
    </div>

    {{-- MODULOS --}}
    <div class="card shadow-sm mb-4">
        <div class="card-header bg-light d-flex justify-content-between align-items-center">
            <strong>Módulos / productos cotizados</strong>
            <span class="badge bg-primary">{{ count($modules) }} módulos</span>
        </div>

        <div class="card-body">
            <div class="row">
                @foreach($modules as $module)
                    <div class="col-md-6 col-xl-4 mb-3">
                        <div class="card h-100 border">
                            <div class="card-body">
                                <div class="d-flex justify-content-between align-items-start mb-2">
                                    <div>
                                        <span class="badge bg-dark">#{{ $module['number'] }}</span>
                                        <strong class="ms-1">{{ $module['reference'] }}</strong>
                                    </div>
                                    <span class="badge bg-light text-dark">{{ $module['environment'] }}</span>
                                </div>

                                <div class="mb-2 text-muted small" style="min-height: 42px;">
                                    {{ $module['description'] }}
                                </div>

                                <div class="row small mb-2">
                                    <div class="col-6">
                                        <span class="text-muted">Cantidad</span><br>
                                        <strong>{{ $module['quantity'] }}</strong>
                                    </div>
                                    <div class="col-6">
                                        <span class="text-muted">Medidas</span><br>
                                        <strong>{{ $module['dx'] }} x {{ $module['dy'] }} x {{ $module['dz'] }}</strong>
                                    </div>
                                </div>

                                <div class="d-flex justify-content-between border-top pt-2">
                                    <div>
                                        <small class="text-muted">PVP</small><br>
                                        <strong>{{ $header['currency'] ?? '' }} {{ $module['pvp'] }}</strong>
                                    </div>
                                    <div class="text-end">
                                        <small class="text-muted">Total</small><br>
                                        <strong class="text-success">{{ $header['currency'] ?? '' }} {{ $module['total'] }}</strong>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                @endforeach
            </div>
        </div>
    </div>

    {{-- DESPIECE --}}
    <h4 class="mb-3">Despiece por módulo</h4>

    @php
        $despiecePorModulo = collect($despiece)->groupBy('module_number');
        $modulesByNumber = collect($modules)->keyBy('number');
    @endphp

    <div class="accordion mb-4" id="accordionDespiece">
        @foreach($despiecePorModulo as $moduleNumber => $rows)
            @php
                $module = $modulesByNumber[$moduleNumber] ?? null;
                $accordionId = 'modulo_' . $moduleNumber;
            @endphp

            <div class="accordion-item mb-2 border rounded shadow-sm">
                <h2 class="accordion-header" id="heading_{{ $accordionId }}">
                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse_{{ $accordionId }}">
                        <div class="w-100 d-flex justify-content-between align-items-center pe-3">
                            <div>
                                <strong>Módulo #{{ $moduleNumber }}</strong>
                                — {{ $rows->first()['module_reference'] ?? '' }}

                                @if($module)
                                    <span class="text-muted ms-2">{{ $module['description'] }}</span>
                                @endif
                            </div>

                            <span class="badge bg-secondary">{{ count($rows) }} piezas</span>
                        </div>
                    </button>
                </h2>

                <div id="collapse_{{ $accordionId }}" class="accordion-collapse collapse" data-bs-parent="#accordionDespiece">
                    <div class="accordion-body p-0">
                        <table class="table table-hover table-sm mb-0 align-middle">
                            <thead class="table-light">
                                <tr>
                                    <th>Tipo</th>
                                    <th>Pieza</th>
                                    <th class="text-center">Cant.</th>
                                    <th class="text-end">Largo</th>
                                    <th class="text-end">Ancho</th>
                                    <th class="text-end">Espesor</th>
                                    <th>Cód. Material</th>
                                    <th>Material</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach($rows as $row)
                                    <tr>
                                        <td>
                                            @if($row['type_code'] == 1)
                                                <span class="badge bg-primary">Pieza</span>
                                            @elseif($row['type_code'] == 2)
                                                <span class="badge bg-warning text-dark">Puerta/Frente</span>
                                            @elseif($row['type_code'] == 4)
                                                <span class="badge bg-info text-dark">Accesorio</span>
                                            @else
                                                <span class="badge bg-secondary">{{ $row['type_name'] }}</span>
                                            @endif
                                        </td>
                                        <td><strong>{{ $row['part'] }}</strong></td>
                                        <td class="text-center">{{ $row['quantity'] }}</td>
                                        <td class="text-end">{{ $row['length'] }}</td>
                                        <td class="text-end">{{ $row['width'] }}</td>
                                        <td class="text-end">{{ $row['thickness'] }}</td>
                                        <td>{{ $row['material_code'] }}</td>
                                        <td style="max-width: 420px;">{{ $row['material'] }}</td>
                                    </tr>
                                @endforeach
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        @endforeach
    </div>

    {{-- ACCIONES --}}
    <div class="card shadow-sm mb-5">
        <div class="card-body d-flex justify-content-between align-items-center">
            <div>
                <strong>¿La información se ve correcta?</strong>
                <div class="text-muted small">Guarda el proyecto para conservar la cabecera, módulos, precios y despiece.</div>
            </div>

            <form action="{{ route('projects.storeFinal') }}" method="POST" class="mb-0">
                @csrf

                <input type="hidden" name="data" value="{{ json_encode([
                    'header' => $header,
                    'headings' => $headings,
                    'modules' => $modules,
                    'despiece' => $despiece,
                    'files' => $files,
                ]) }}">

                <button class="btn btn-success">
                    Guardar Proyecto
                </button>
            </form>
        </div>
    </div>

</div>
@endsection
