@extends('layouts.app')

@section('content')
@php
    $activePercent = $totalUsers > 0 ? round(($activeUsers / $totalUsers) * 100) : 0;
    $inactivePercent = $totalUsers > 0 ? round(($inactiveUsers / $totalUsers) * 100) : 0;
@endphp

<div class="mb-6 flex items-center justify-between">
    <div>
        <h1 class="text-2xl font-bold text-gray-800">Dashboard</h1>

        @if ($isSuperAdmin)
            <p class="text-sm text-gray-500">Vista general de todas las empresas.</p>
        @else
            <p class="text-sm text-gray-500">Vista de la empresa {{ auth()->user()->company->name ?? 'Sin empresa' }}.</p>
        @endif
    </div>

    <div class="text-sm text-gray-500">
        {{ now()->format('d/m/Y H:i') }}
    </div>
</div>

@if ($isSuperAdmin)
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-6">
        <div class="bg-white rounded-xl shadow p-5">
            <p class="text-sm text-gray-500">Empresas</p>
            <h2 class="text-3xl font-bold text-gray-800 mt-2">{{ $totalCompanies }}</h2>
        </div>

        <div class="bg-white rounded-xl shadow p-5">
            <p class="text-sm text-gray-500">Empresas activas</p>
            <h2 class="text-3xl font-bold text-green-700 mt-2">{{ $activeCompanies }}</h2>
        </div>

        <div class="bg-white rounded-xl shadow p-5">
            <p class="text-sm text-gray-500">Usuarios totales</p>
            <h2 class="text-3xl font-bold text-indigo-700 mt-2">{{ $totalUsers }}</h2>
        </div>

        <div class="bg-white rounded-xl shadow p-5">
            <p class="text-sm text-gray-500">Roles</p>
            <h2 class="text-3xl font-bold text-purple-700 mt-2">{{ $rolesCount }}</h2>
        </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <div class="bg-white rounded-xl shadow p-6">
            <h3 class="text-lg font-semibold text-gray-800 mb-4">Estado de usuarios</h3>

            <div class="space-y-4">
                <div>
                    <div class="flex justify-between text-sm mb-1">
                        <span>Activos</span>
                        <span>{{ $activePercent }}%</span>
                    </div>
                    <div class="w-full bg-gray-200 rounded-full h-3">
                        <div class="bg-green-500 h-3 rounded-full" style="width: {{ $activePercent }}%"></div>
                    </div>
                </div>

                <div>
                    <div class="flex justify-between text-sm mb-1">
                        <span>Inactivos</span>
                        <span>{{ $inactivePercent }}%</span>
                    </div>
                    <div class="w-full bg-gray-200 rounded-full h-3">
                        <div class="bg-red-500 h-3 rounded-full" style="width: {{ $inactivePercent }}%"></div>
                    </div>
                </div>
            </div>
        </div>

        <div class="lg:col-span-2 bg-white rounded-xl shadow p-6">
            <h3 class="text-lg font-semibold text-gray-800 mb-4">Empresas y usuarios</h3>

            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Empresa</th>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Estado</th>
                            <th class="px-4 py-3 text-right text-xs font-medium text-gray-500 uppercase">Usuarios</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        @forelse ($companies as $company)
                            <tr>
                                <td class="px-4 py-3 text-sm font-medium text-gray-900">{{ $company->name }}</td>
                                <td class="px-4 py-3 text-sm text-gray-600">{{ ucfirst($company->status) }}</td>
                                <td class="px-4 py-3 text-sm text-right text-gray-600">{{ $company->users_count }}</td>
                            </tr>
                        @empty
                            <tr>
                                <td colspan="3" class="px-4 py-6 text-center text-gray-500">No hay empresas registradas.</td>
                            </tr>
                        @endforelse
                    </tbody>
                </table>
            </div>
        </div>
    </div>
@else
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-6">
        <div class="bg-white rounded-xl shadow p-5">
            <p class="text-sm text-gray-500">Empresa</p>
            <h2 class="text-xl font-bold text-gray-800 mt-2">{{ auth()->user()->company->name ?? 'Sin empresa' }}</h2>
        </div>

        <div class="bg-white rounded-xl shadow p-5">
            <p class="text-sm text-gray-500">Usuarios</p>
            <h2 class="text-3xl font-bold text-indigo-700 mt-2">{{ $totalUsers }}</h2>
        </div>

        <div class="bg-white rounded-xl shadow p-5">
            <p class="text-sm text-gray-500">Usuarios activos</p>
            <h2 class="text-3xl font-bold text-green-700 mt-2">{{ $activeUsers }}</h2>
        </div>

        <div class="bg-white rounded-xl shadow p-5">
            <p class="text-sm text-gray-500">Usuarios inactivos</p>
            <h2 class="text-3xl font-bold text-red-700 mt-2">{{ $inactiveUsers }}</h2>
        </div>
    </div>

    <div class="bg-white rounded-xl shadow p-6 mb-6">
        <h3 class="text-lg font-semibold text-gray-800 mb-4">Estado de usuarios de la empresa</h3>

        <div class="space-y-4">
            <div>
                <div class="flex justify-between text-sm mb-1">
                    <span>Activos</span>
                    <span>{{ $activePercent }}%</span>
                </div>
                <div class="w-full bg-gray-200 rounded-full h-3">
                    <div class="bg-green-500 h-3 rounded-full" style="width: {{ $activePercent }}%"></div>
                </div>
            </div>

            <div>
                <div class="flex justify-between text-sm mb-1">
                    <span>Inactivos</span>
                    <span>{{ $inactivePercent }}%</span>
                </div>
                <div class="w-full bg-gray-200 rounded-full h-3">
                    <div class="bg-red-500 h-3 rounded-full" style="width: {{ $inactivePercent }}%"></div>
                </div>
            </div>
        </div>
    </div>
@endif

<div class="bg-white rounded-xl shadow p-6 mt-6">
    <h3 class="text-lg font-semibold text-gray-800 mb-4">Actividad reciente</h3>

    <div class="space-y-3">
        @forelse ($logs as $log)
            <div class="flex items-start gap-3 border-b pb-3">
                <div class="w-9 h-9 rounded-full bg-indigo-100 text-indigo-700 flex items-center justify-center">
                    &#128203;
                </div>
                <div>
                    <p class="text-sm text-gray-700">{{ $log->description }}</p>
                    <p class="text-xs text-gray-400">
                        {{ $log->causer->name ?? 'Sistema' }} - {{ $log->created_at->format('d/m/Y H:i') }}
                    </p>
                </div>
            </div>
        @empty
            <p class="text-sm text-gray-500">Sin actividad registrada.</p>
        @endforelse
    </div>
</div>
@endsection